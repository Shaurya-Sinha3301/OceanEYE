import type { Metadata } from 'next'
import { Inter } from 'next/font/google'
import './globals.css'

const inter = Inter({ subsets: ['latin'] })

export const metadata: Metadata = {
  title: 'DNABERT-2 | AI-Driven Deep-Sea eDNA Analysis',
  description: 'Revolutionary AI pipeline for identifying eukaryotic taxa and assessing biodiversity directly from raw eDNA reads in deep-sea ecosystems.',
}

export default function RootLayout({
  children,
}: {
  children: React.ReactNode
}) {
  return (
    <html lang="en">
      <body className={inter.className}>{children}</body>
    </html>
  )
}